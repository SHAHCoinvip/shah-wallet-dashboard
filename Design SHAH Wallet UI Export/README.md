
  # Design SHA Wallet Dashboard UI

  This is a code bundle for Design SHA Wallet Dashboard UI. The original project is available at https://www.figma.com/design/u8r8PJzm7RWO1RfeT4ghcL/Design-SHA-Wallet-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  